# 复制为 .env.local 并填入配置（本地开发用，绝不提交）。
# CI / Cloudflare Pages 构建时也可直接在环境变量中注入同名变量，进程环境变量优先。
#
# —— 站点信息（必配 SITE_URL；用于页面标题、描述与 RSS/sitemap 规范地址）——
#   SITE_TITLE       —— 站点标题
#   SITE_DESCRIPTION —— 站点描述
#   SITE_URL         —— 部署后的线上域名，如 https://tutorial-blog.pages.dev
#                       （https:// 开头，结尾不要带斜杠；RSS/sitemap 据此拼文章链接）
SITE_TITLE=
SITE_DESCRIPTION=
SITE_URL=

# —— 图床上传（腾讯云 COS，可选）——
# 参考：https://console.cloud.tencent.com/cos
#
# 获取方式：
#   COS_SECRET_ID / COS_SECRET_KEY —— 腾讯云"访问管理 CAM"→ 新建密钥
#       （建议建一个只授予该 Bucket PutObject 权限的子账号，勿用主账号密钥）；
#     或控制台右上角头像 → 访问管理 → 访问密钥 → API 密钥管理。
#   COS_BUCKET  —— Bucket 名，形如 `博客名-appid`（在 COS 控制台的 Bucket 概览可见）
#   COS_REGION  —— Bucket 所在地域，如 `ap-guangzhou`
#   COS_DOMAIN  —— 对外访问域名，绑到 COS/CDN 的自定义域名，如 https://img.example.com
COS_SECRET_ID=
COS_SECRET_KEY=
COS_BUCKET=
COS_REGION=
COS_DOMAIN=

# —— Giscus 评论（可选，不配则文章页不显示评论区）——
# 在 https://giscus.app 选择仓库、选好分类后，从生成的 <script> 中抄取对应值。
# 前提：仓库已开启 Discussions（Settings → Features → Discussions ✓）。
#   GISCUS_REPO        —— 仓库名，格式 `owner/repo`（与 GitHub 地址一致）
#   GISCUS_REPO_ID     —— 仓库的 graphql id（giscus.app 页面会给出）
#   GISCUS_CATEGORY    —— 讨论分类名，默认 `Announcements`
#   GISCUS_CATEGORY_ID —— 该分类对应的 id（giscus.app 页面会给出）
#   GISCUS_MAPPING     —— 评论与页面的映射方式，默认 `pathname`
GISCUS_REPO=
GISCUS_REPO_ID=
GISCUS_CATEGORY=
GISCUS_CATEGORY_ID=
GISCUS_MAPPING=
